/// Unified enums file for the Medication Manager
/// Contains all enumeration types used throughout the application

/// Enum representing the role of medical staff
enum StaffRole {
  doctor,
  nurse,
}

/// Enum representing the form of medication
enum MedicationForm {
  tablet,
  liquid,
  injection,
}

/// Enum representing the severity level of an allergy
enum AllergySeverity {
  low,
  medium,
  high,
}
